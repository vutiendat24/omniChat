# Docker profile — override internal hostnames
spring:
  kafka:
    consumer:
      bootstrap-servers: kafka:29092

  mail:
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}

app:
  frontend-url: ${FRONTEND_URL:http://localhost:5173}
